version: 2

models:
  - name: stg_orders
    description: "orders table data"
    columns:
      - name: O_CUSTKEY
        description: "Customer key"
        tests:
          - not_null:
              config: 
                where: "O_ORDERSTATUS = 'O'"