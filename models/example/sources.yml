version: 2

sources:
  - name: dbt_samples
    database: RAW
    schema: GLOBALMART
    tables:
      - name: customer
        columns:
          - name: C_CUSTKEY
            tests:
              - not_null
              

